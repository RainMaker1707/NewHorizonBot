const Discord = require('discord.js');


module.exports = {
  name: 'close',
  value: "N/A",
  description: "Close the channel if it is a ticket channel. Require moderator access.",
  options: [],
  execute(interaction, client, callback) {
      console.log("CheckID slashed")
  }
}
